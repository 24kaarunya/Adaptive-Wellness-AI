# Database (SQLite for easy setup)
# No external database server required - uses local file
DATABASE_URL="file:./dev.db"

# NextAuth Configuration
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Google OAuth (OPTIONAL - can sign in with email/password)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# OpenAI API (REQUIRED for AI Agents to function)
# Get free tier API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-your-openai-api-key-here"

# App Settings
NODE_ENV="development"
